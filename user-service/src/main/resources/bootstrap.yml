spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888  # Fetch configuration from Config Server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
